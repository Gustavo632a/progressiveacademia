import { ArrowUpRight, MoveUpRight } from 'lucide-react';
import { modalities } from '../data/modalities';
import { getContactUrl } from '../data/config';
import { ContactButton, SectionHeading } from './UI';
export default function Modalities() {
  const confirmed = modalities.filter(m => m.confirmed);
  return <section id="modalidades" className="section section-surface"><div className="container"><div className="section-top" data-reveal><SectionHeading eyebrow="SEU TREINO, SEU RITMO" title="ENCONTRE SEU JEITO" accent="DE EVOLUIR.">Escolha uma modalidade que combine com seu objetivo.</SectionHeading><a className="text-link" href={getContactUrl('Olá! Gostaria de conhecer as modalidades disponíveis na Progressive.')} target="_blank" rel="noopener noreferrer">Conhecer modalidades<ArrowUpRight size={18} /></a></div><div className="modalities-grid">{confirmed.map((m, i) => <article className="modality-card" data-reveal key={m.id}><img src={m.image} alt={`Treino de ${m.name.toLowerCase()} — imagem ilustrativa`} width="900" height="1100" loading="lazy" /><span className="card-number">0{i + 1} /</span><div className="modality-content"><span className="eyebrow">{m.tag}</span><h3>{m.name}</h3><p>{m.description}</p><a href={getContactUrl(`Olá! Gostaria de saber mais sobre ${m.name.toLowerCase()} na Progressive.`)} target="_blank" rel="noopener noreferrer">Quero conhecer<ArrowUpRight size={20} /></a></div></article>)}<article className="discovery-card" data-reveal><span className="eyebrow">NOVAS POSSIBILIDADES</span><MoveUpRight size={76} strokeWidth={1} /><h3>QUAL É O SEU<br /><span>PRÓXIMO DESAFIO?</span></h3><p>Seu objetivo é o ponto de partida. Converse com a equipe e conheça as modalidades e a programação disponíveis.</p><ContactButton outline message="Olá! Quero conhecer as modalidades e a programação atual da Progressive.">Encontre seu treino</ContactButton><span className="discovery-footer">UM PASSO DE CADA VEZ. SEMPRE EM FRENTE.</span></article></div><p className="image-note">Fotografias ilustrativas. Consulte a equipe para conferir a programação atual.</p></div></section>;
}
