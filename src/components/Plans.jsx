import { ArrowUpRight, Check, Sparkles } from 'lucide-react';
import { plans } from '../data/plans';
import { ContactButton, SectionHeading } from './UI';
export default function Plans() {
  const available = plans.filter(plan => plan.confirmed);
  return <section className="section section-surface" id="planos"><div className="container plans-layout"><div data-reveal><SectionHeading eyebrow="UM NOVO COMEÇO" title="ESCOLHA COMO" accent="COMEÇAR.">O melhor plano é dar o primeiro passo.<br />Vamos encontrar a opção para você.</SectionHeading><span className="plans-decoration" aria-hidden="true"><ArrowUpRight size={130} strokeWidth={.7} /></span></div><div className="plans-cards" data-reveal>{available.length ? available.map(plan => <article key={plan.name} className={`plan-card ${plan.featured ? 'featured' : ''}`}><h3>{plan.name}</h3>{plan.price != null && <p className="plan-price">{new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(plan.price)}</p>}<ul>{plan.features.map(feature => <li key={feature}><Check size={16} />{feature}</li>)}</ul><ContactButton message={`Olá! Gostaria de conhecer o ${plan.name}.`}>Conhecer plano</ContactButton></article>) : <article className="plan-card"><div className="plan-icon"><Sparkles size={26} /></div><span className="eyebrow">SEU PROGRESSO COMEÇA COM UMA CONVERSA</span><h3>Consulte nossos planos</h3><p>Fale com nossa equipe para conhecer as opções disponíveis.</p><ContactButton message="Olá! Gostaria de conhecer os planos disponíveis na Progressive Academia.">Consultar planos</ContactButton><span className="plan-footnote">Converse com quem vai acompanhar sua evolução.</span></article>}</div></div></section>;
}
