import { ArrowUpRight, MapPin } from 'lucide-react';
import { units } from '../data/units';
import { ContactButton, SectionHeading } from './UI';
export default function Units() {
  return <section id="unidades" className="section section-surface"><div className="container"><SectionHeading eyebrow="NOSSO MOVIMENTO CONTINUA" title="CADA VEZ MAIS" accent="PERTO DE VOCÊ." /><div className="units-grid">{units.map(unit => <article key={unit.id} className={`unit-card ${unit.soon ? 'unit-soon' : ''}`} data-reveal>{unit.image && <img className="unit-image" src={unit.image} alt="Ambiente de academia — fotografia ilustrativa" loading="lazy" width="900" height="650" />}<div className="unit-content"><div className="unit-top"><span className="eyebrow">UNIDADE {unit.zone.toUpperCase()}</span><span className={`status ${unit.soon ? 'status-soon' : ''}`}><i />{unit.status}</span></div><span className="unit-outline" aria-hidden="true">{unit.soon ? '02' : '01'}</span><div className="unit-info"><h3>{unit.name}</h3><p><MapPin size={15} />{unit.name} — {unit.city}</p>{unit.soon && <p className="unit-description">A Progressive está chegando à Zona Sul.<br />Um novo espaço. A mesma vontade de evoluir.</p>}<div className="unit-actions">{unit.soon ? <ContactButton message="Olá! Gostaria de receber informações sobre a nova unidade Progressive Valentina.">Quero receber novidades</ContactButton> : <><ContactButton>Falar com a unidade</ContactButton><a className="text-link" href={unit.mapsUrl} target="_blank" rel="noopener noreferrer">Ver localização<ArrowUpRight size={16} /></a></>}</div></div></div></article>)}</div></div></section>;
}
